# change_11.md — фото + редизайн viewer

## Что сделано

### Фотографии
- Принял 977 папок / 2964 .webp из `/home/workspace/photos/`
- Симлинк `Projects/Cian/photos → /home/workspace/photos`
- Скрипт `seed_photos.py` заполнил таблицу `offer_photos` (2964 строки)
- На каждое объявление 3 фото; предпочтение не-планировке при выборе обложки

### Редизайн viewer.py

**Архитектура CSS:**
- Весь CSS вынесен в константу `SHARED_CSS` и отдаётся через `/style.css` (кэш 1 ч)
- Оба шаблона (`LIST_HTML`, `DETAIL_HTML`) подключают его через `<link>`
- CSS-переменные для цветов/теней/радиусов — легко перекрашивать

**Список объявлений:**
- Sticky header: логотип + count badge (976 объявлений)
- Filter bar: поиск, комнаты, цена до, тип жилья, сортировка
- Luxury-гистограмма с градиентом золото→зелёный→фиолетовый
- Карточки: фото-hero 210px, цена поверх фото (blur overlay), badges «Новостройка» и «−X% модель», pills (комнаты/м²/этаж), метро-тег, luxury-бар
- Hover: подъём карточки + тень + scale фото

**Детальная страница:**
- Галерея: большое фото 460px + thumbnails с плавным switch (JS fade opacity)
- Sticky price card: цена/м², 4 key specs, метро-блок, ML-блок зелёный, два luxury gauge с градиентными барами, кнопка «Открыть на Cian»
- Full-specs grid 3 колонки: 16 характеристик
- Форматированное описание

## Обоснование
Предыдущий дизайн был минимальным (flex+border, нет фото). Теперь — premium PropTech уровень:
приоритет фото, ML/luxury данные визуально отделены, адаптивная сетка.
